extends layout

block content
    
    if message
        p #{message}

    form(action="/new_comment_save/#{postId}", method="post")
        label(for="content") Content :
        input(type="text", name="content")
        br
        button(type="submit") Send